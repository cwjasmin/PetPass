name: petpass
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  dynamic_sdk: ^0.0.1-alpha.3
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_neat_and_clean_calendar: ^0.4.15
  flutter_svg: ^2.0.10+1
  reown_appkit: ^1.0.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
   - .env
   - assets/images/